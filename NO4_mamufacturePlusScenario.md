# ข้อ 4: ManufacturePlus Scenario

## Functional Requirements

### 1. Employee Self-Service Leave Dashboard

พนักงานสามารถตรวจสอบวันลาคงเหลือ วันลาที่ใช้แล้ว และวันลาที่รออนุมัติได้ด้วยตนเอง โดยข้อมูลอัปเดตจากระบบกลาง

### 2. Online Leave Request and Approval

พนักงานสามารถยื่นคำขอลาผ่านระบบ หัวหน้าอนุมัติหรือปฏิเสธได้ และพนักงานตรวจสอบสถานะคำขอได้โดยไม่ต้องถาม HR

### 3. Leave History and Notification

พนักงานสามารถดูประวัติการลา พร้อมรับการแจ้งเตือนเมื่อคำขอได้รับอนุมัติ ถูกปฏิเสธ หรือยอดวันลามีการเปลี่ยนแปลง

## Data Cleansing

### กรณีชื่อพนักงานพิมพ์ผิด

1. ตัดช่องว่างเกินและปรับรูปแบบชื่อให้เหมือนกัน
2. เทียบชื่อกับ Employee Master โดยใช้ Employee ID ช่วยตรวจสอบ
3. ถ้าชื่อไม่ตรง ให้ HR ยืนยันก่อนแก้เป็นชื่อที่ถูกต้อง

### กรณี Employee ID ซ้ำ

1. ใช้ Remove Duplicates หรือค้นหา Employee ID ที่ซ้ำใน Excel
2. เปรียบเทียบชื่อ เลขบัตรประชาชน หรือ Email ว่าเป็นคนเดียวกันหรือไม่
3. ถ้าเป็นคนเดียวกัน ให้รวมข้อมูลไว้ใน Employee ID เดียว
4. ถ้าเป็นคนละคน ให้สร้าง Employee ID ใหม่ให้คนใดคนหนึ่ง
5. กำหนด Employee ID เป็น Unique เพื่อป้องกันข้อมูลซ้ำในอนาคต
